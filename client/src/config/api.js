export default {
  baseUrl: "http://localhost:3000/api/",
  resources: {
    Postes: {
      controller: "postes"
    },
    Operators: {
      controller: "operators"
    },
    Products: {
      controller: "products"
    }
  }
};
